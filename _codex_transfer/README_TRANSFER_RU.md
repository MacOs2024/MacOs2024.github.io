# VoltCalc: перенос локального аудита в PR №1

## Зафиксированное состояние

- Локальная база изменений: `111a7fd3c79d62cfe9bc3d27bb7544fb3fe3f372`
- Актуальный head PR №1 на момент упаковки: `f399042`
- Рабочая ветка PR: `claude/voltcalc-audit-remediation`
- Локально изменено 119 отслеживаемых файлов и добавлено 2 новых файла.
- Корневые `*.html` являются результатом генерации. Источники находятся в `generator/`.

## Содержимое

- `00_SOURCE_ONLY_RECOMMENDED.patch` — рекомендуемый перенос источников генератора, тестов, workflow, зависимостей и документации. Корневые HTML намеренно исключены.
- `01_tracked_changes.patch` — полный резервный diff всех отслеживаемых файлов, включая пересобранные HTML. Использовать только для сверки или восстановления отдельных фрагментов.
- `02_add_node_version.patch` — добавление `.node-version`; на актуальной ветке файл уже может существовать.
- `03_add_audit_metadata.patch` — добавление `generator/audit_metadata.py`; на актуальной ветке файл уже может существовать.

## Безопасный порядок

1. Убедиться, что работа начата с актуального head ветки PR №1 и что `main` не выбран.
2. Проверить `gh auth status`, `git status`, `git rev-parse HEAD` и `gh pr view 1`.
3. Сначала сравнить файлы из архива с текущей веткой: часть правок уже попала в коммиты `237ca8b` и `f399042`.
4. Применять `00_SOURCE_ONLY_RECOMMENDED.patch` через трёхстороннее слияние. Контрольная проба поверх `f399042` дала ровно 6 конфликтов: `ENGINEERING_AUDIT.md`, `generator/common.py`, `generator/data_f.py`, `generator/data_g.py`, `tests/e2e/smoke.spec.js`, `tests/verify.mjs`.
5. Разрешать конфликты вручную по смыслу. Не выбирать целиком одну сторону и не применять полный patch поверх всех корневых HTML.
6. Проверить/добавить `.node-version` и `generator/audit_metadata.py` отдельно, только если актуальная ветка ещё не содержит эквивалентных версий.
7. Пересобрать сайт штатным генератором; корневые HTML должны получиться из источников.
8. Запустить чистую установку зависимостей, модульные проверки, воспроизводимость сборки, `npm audit` и Playwright 34/34 с Chromium.
9. Проверить diff: никаких случайных файлов, кэшей, артефактов или изменений `main`.
10. Создать один осмысленный commit и выполнить обычный push только в `claude/voltcalc-audit-remediation`; force-push запрещён.
11. Обновить существующий PR №1, но не создавать новый PR и не выполнять merge.
12. В финале сообщить точные SHA, результаты тестов, URL PR и перечень разрешённых конфликтов.

Если безопасное разрешение хотя бы одного конфликта неоднозначно, остановиться до commit/push и перечислить спорные файлы.
