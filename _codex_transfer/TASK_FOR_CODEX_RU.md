# Задача: завершить шестой шаг аудита VoltCalc

Работай в репозитории `MacOs2024/MacOs2024.github.io`.

## Цель

Перенести подготовленные изменения аудита поверх актуальной head-ветки PR №1, выполнить полную проверку и, только при зелёном результате, обновить существующий PR №1.

## Исходные ветки

- Эта временная ветка содержит только материалы переноса в каталоге `_codex_transfer/`.
- Целевая ветка существующего PR №1: `claude/voltcalc-audit-remediation`.
- Зафиксированный head PR при подготовке материалов: `f399042b8a03026810455333fa3d1122477469f2`.

Перед началом повторно получи актуальный head PR №1. Если он новее `f399042`, работай с его актуальным потомком и учитывай новые изменения.

## Строгие ограничения

- не изменяй `main`;
- не выполняй merge;
- не создавай новый PR;
- не используй force-push;
- не выполняй `reset --hard`, `clean` или перезапись ветки;
- не публикуй изменения, пока все проверки не пройдены;
- при неоднозначном инженерном конфликте остановись и перечисли проблему.

## Материалы

- `_codex_transfer/00_SOURCE_ONLY_RECOMMENDED.patch` — основной перенос;
- `_codex_transfer/02_add_node_version.patch` — контроль для `.node-version`;
- `_codex_transfer/03_add_audit_metadata.patch` — контроль для `generator/audit_metadata.py`;
- `_codex_transfer/README_TRANSFER_RU.md` — подробности происхождения и проверки пакета.

## Порядок работы

1. Прочитай все материалы в `_codex_transfer/`.
2. Проверь `git status`, текущую ветку, remotes, `gh --version`, `gh auth status` и актуальное состояние PR №1.
3. Переключись на актуальную `claude/voltcalc-audit-remediation` и убедись, что HEAD равен актуальному head PR №1.
4. Выполни контрольное применение `_codex_transfer/00_SOURCE_ONLY_RECOMMENDED.patch` без публикации.
5. Поверх `f399042` при подготовке ожидались шесть смысловых конфликтов:
   - `ENGINEERING_AUDIT.md`;
   - `generator/common.py`;
   - `generator/data_f.py`;
   - `generator/data_g.py`;
   - `tests/e2e/smoke.spec.js`;
   - `tests/verify.mjs`.
6. Разреши конфликты вручную по смыслу, сохраняя одновременно изменения из существующей ветки PR и подготовленный аудит. Не выбирай целиком `ours` или `theirs`.
7. Отдельно сравни `.node-version` и `generator/audit_metadata.py` с patch-файлами 02 и 03. Не перезаписывай эквивалентную или более новую версию.
8. Пересобери корневые HTML только штатным генератором.
9. Проверь отсутствие конфликтных маркеров, временного каталога `_codex_transfer/` и случайных файлов в итоговом diff.
10. Выполни:
    - чистую установку зависимостей;
    - `npm test` — ожидается 8110/8110;
    - `npm audit` — ожидается 0 уязвимостей;
    - две последовательные сборки и сравнение результатов;
    - Playwright с реальным Chromium — ожидается 34/34;
    - финальную проверку `git diff` и `git status`.
11. Сохрани инженерные статусы: 52 `agent-reviewed`, 47 `estimate`, 0 `verified`. Зелёные тесты не являются независимой инженерной сертификацией.
12. Только если всё прошло: создай один осмысленный commit и выполни обычный push без `--force` только в `claude/voltcalc-audit-remediation`.
13. Убедись, что обновился существующий PR №1. Не создавай новый PR и не выполняй merge.
14. Добавь в PR №1 краткую сводку: перенос, шесть разрешённых конфликтов, результаты тестов, новый SHA и ограничения инженерной верификации.

## Финальный ответ

Укажи исходный и новый SHA, URL PR №1, результаты каждой проверки, список разрешённых конфликтов и подтверждение, что `main` не изменён, force-push и merge не выполнялись. Если авторизации недостаточно или конфликт нельзя разрешить надёжно, ничего не отправляй и точно опиши блокер.
